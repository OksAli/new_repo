from math import ceil
def square(x):
       x = "Сторона"
def ceil_mult(x):
    return ceil(x*x)

x = float(input("x: "))
result = square(x)
print(f'Округленное в большую сторону произведение - {ceil_mult(x)}')